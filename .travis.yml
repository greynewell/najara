language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install pipenv
  - pipenv install
# command to run tests
script:
  - python3 -m pytest --cov tests/ -v
after_success:
  - bash <(curl -s https://codecov.io/bash)
